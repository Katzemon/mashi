// Local server
const PORT = 3000;

// GIFs
const defaultGifWidth = 552;
const defaultGifHeight = 736;
const defaultTraitWidth = 380;
const defaultTraitHeight = 600;

const frameDelayMs = 30;
const captureFps = 33.33;
const playbackFps = 30;

module.exports = {
    PORT,
    defaultGifWidth,
    defaultGifHeight,
    defaultTraitWidth,
    defaultTraitHeight,
    frameDelayMs,
    captureFps,
    playbackFps
};